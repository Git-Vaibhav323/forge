# Base URL of the backend API gateway (see backend/README.md).
# Leave unset to run the frontend fully on mock data.
# For live M1 + M2 (projects + uploads):
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
