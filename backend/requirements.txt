# Core API
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.8.2
pydantic-settings==2.3.4
python-multipart==0.0.9

# PostgreSQL (M1 — project persistence)
sqlalchemy==2.0.31
psycopg[binary]==3.2.13
alembic==1.13.2

# Object storage — MinIO locally, or Supabase Storage (S3 API) in production
boto3==1.34.162
botocore==1.34.162
minio==7.2.7

# PDF text extraction (M4 — evidence-service). Pure Python, no system libs.
pypdf==4.3.1

# PDF report generation (M8 — generation-service). Pure Python, no system libs.
fpdf2==2.8.2

# Unit normalization (M5 — review-service). Pure Python, no system libs.
# 0.24.4+ required: 0.24.3 fails to import against flexparser 0.4.
pint==0.24.4

# Testing
pytest==8.3.2
pytest-asyncio==0.23.8
httpx==0.27.0

# Nameplate OCR (M7) — OPTIONAL. Only needed for OCR_PROVIDER=tesseract, which
# also requires the tesseract binary on the system. The default OCR_PROVIDER=off
# needs none of this: images are stored and read no facts.
# pytesseract==0.3.13
# pillow==10.4.0

# Add as each stage is implemented (see plan.md):
# pgvector==0.3.2           # vector column type
# redis==5.0.7              # event bus / job queue
# celery==5.4.0             # background workers
# pymupdf==1.24.9           # PDF text extraction
# ocrmypdf==16.4.3          # OCR fallback for scanned PDFs
# langgraph==0.1.14         # workflow orchestration
